import React,{useEffect,useState}from'react';import{LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer}from'recharts';import'./style.css';
const api=(u,o={})=>fetch('/api'+u,{headers:{'Content-Type':'application/json',...(o.headers||{})},...o}).then(async r=>{const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Erro');return d}),now=()=>new Date().toISOString().slice(0,16);
const Field=({label,k,x,f,type='text',opts})=><label>{label}{type==='select'?<select value={x[k]??''} onChange={e=>f(k,e.target.value)}><option value="">—</option>{opts?.map(o=><option key={o[0]} value={o[0]}>{o[1]}</option>)}</select>:type==='textarea'?<textarea value={x[k]??''} onChange={e=>f(k,e.target.value)}/>:<input type={type} value={x[k]??''} onChange={e=>f(k,type==='number'?Number(e.target.value):e.target.value)}/>}</label>;
function Auth({onLogin}){const[mode,setMode]=useState('login'),[x,setX]=useState({}),[error,setError]=useState(''),busy=mode==='register';const f=(k,v)=>setX({...x,[k]:v});return <div className="auth-page"><div className="auth-hero"><div className="auth-brand"><span>⚽</span> BETTRACKER</div><div className="hero-content"><span className="eyebrow">O teu centro de controlo de apostas</span><h1>Dados claros.<br/><strong>Decisões melhores.</strong></h1><p>Centraliza apostas, casas, banca, bónus e resultados num único painel.</p><div className="feature-grid"><div><b>📊</b><span>Dashboard & ROI</span></div><div><b>🎯</b><span>Análise por mercado</span></div><div><b>💶</b><span>Gestão da banca</span></div><div><b>🎁</b><span>Controlo de bónus</span></div><div><b>🌍</b><span>Competições globais</span></div><div><b>📈</b><span>Histórico e evolução</span></div></div></div></div><div className="auth-panel"><div className="authbox"><div className="mobile-brand">⚽ BETTRACKER</div><div className="auth-kicker">{busy?'COMEÇAR AGORA':'BEM-VINDO DE VOLTA'}</div><h2>{busy?'Criar a tua conta':'Entrar na tua conta'}</h2><p className="muted">{busy?'Regista-te para começares a acompanhar a tua banca.':'Acede ao teu painel pessoal de apostas.'}</p>{busy&&<Field label="Nome" k="name" x={x} f={f}/>}<Field label="Email" k="email" type="email" x={x} f={f}/><Field label="Password" k="password" type="password" x={x} f={f}/>{error&&<div className="error">{error}</div>}<button className="primary full auth-submit" onClick={()=>api('/auth/'+mode,{method:'POST',body:JSON.stringify(x)}).then(r=>onLogin(r.user)).catch(e=>setError(e.message))}>{busy?'Criar conta':'Entrar'} <span>→</span></button><button className="linkbtn" onClick={()=>{setMode(busy?'login':'register');setError('')}}>{busy?'Já tenho conta':'Ainda não tens conta? Criar conta'}</button></div><div className="auth-foot">Os teus dados ficam associados à tua conta e guardados no teu ambiente.</div></div></div>}
function App(){const[user,setUser]=useState(null),[checking,setChecking]=useState(true),[tab,setTab]=useState('dashboard'),[bets,setBets]=useState([]),[houses,setHouses]=useState([]),[tx,setTx]=useState([]),[bonuses,setBonuses]=useState([]),[countries,setCountries]=useState([]),[competitions,setCompetitions]=useState([]),[markets,setMarkets]=useState([]),[stats,setStats]=useState({});useEffect(()=>api('/auth/me').then(r=>setUser(r.user)).catch(()=>{}).finally(()=>setChecking(false)),[]);const load=()=>Promise.all([api('/bets'),api('/houses'),api('/transactions'),api('/bonuses'),api('/countries'),api('/competitions'),api('/markets'),api('/stats')]).then(([a,b,c,d,e,f,g,h])=>{setBets(a);setHouses(b);setTx(c);setBonuses(d);setCountries(e);setCompetitions(f);setMarkets(g);setStats(h)});useEffect(()=>{if(user)load()},[user]);if(checking)return <div className="auth-page"><div className="auth-loading">⚽ BETTRACKER<br/><span>A carregar…</span></div></div>;if(!user)return <Auth onLogin={setUser}/>;const hasDeposit=tx.some(x=>x.type==='deposit'&&Number(x.amount)>0);return <><header><b>⚽ BETTRACKER</b><span className="user">{user.name}</span><nav>{[['dashboard','Dashboard'],['analysis','Análise'],['bet','Nova aposta'],['bets','Apostas'],['houses','Casas'],['money','Movimentos'],['bonus','Bónus'],...(user.role==='admin'?[['admin','Utilizadores'],['config','Configuração']]:[])].map(([id,n])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}>{n}</button>)}</nav><a href="/api/export">CSV</a><button onClick={()=>api('/auth/logout',{method:'POST'}).then(()=>setUser(null))}>Sair</button></header><main>{!hasDeposit&&<div className="notice"><b>Primeiro passo:</b> adiciona uma casa de apostas e regista o depósito inicial em <b>Movimentos</b>. Isto permite acompanhar corretamente o saldo e o P&L.</div>}{tab==='dashboard'&&<Dashboard stats={stats} bets={bets} houses={houses}/>} {tab==='bet'&&<BetForm bets={bets} houses={houses} countries={countries} competitions={competitions} markets={markets} done={()=>{load();setTab('bets')}}/>}{tab==='bets'&&<BetList bets={bets} houses={houses} countries={countries} competitions={competitions} markets={markets} done={load}/>} {tab==='houses'&&<HouseForm houses={houses} done={load}/>} {tab==='money'&&<Money houses={houses} tx={tx} done={load}/>} {tab==='bonus'&&<Bonus houses={houses} bonuses={bonuses} done={load}/>} {tab==='admin'&&user.role==='admin'&&<Admin/>}{tab==='config'&&user.role==='admin'&&<Config/>}</main></>}
function Analysis({bets,houses,countries,competitions,markets}){const[f,setF]=useState({limit:10}),[data,setData]=useState(null),[loading,setLoading]=useState(false);const run=()=>{setLoading(true);const p=new URLSearchParams(Object.entries(f).filter(([,v])=>v!==''&&v!=null));api('/analysis?'+p).then(setData).catch(()=>setData(null)).finally(()=>setLoading(false))};useEffect(run,[]);const fmt=n=>Number(n||0).toFixed(2);const Table=({title,rows})=><section><h2>{title}</h2>{!rows?.length?<div className="empty">Sem dados para os filtros selecionados.</div>:<div className="tablewrap"><table><thead><tr><th>Nome</th><th>Apostas</th><th>Resolvidas</th><th>Stake</th><th>P/L</th><th>ROI</th><th>Win rate</th></tr></thead><tbody>{rows.map((r,i)=><tr key={i}><td>{r.name}</td><td>{r.bets}</td><td>{r.resolved}</td><td>{fmt(r.stake)} €</td><td>{fmt(r.pnl)} €</td><td>{fmt(r.roi)}%</td><td>{fmt(r.winRate)}%</td></tr>)}</tbody></table></div>}</section>;return <><div className="dashboard-head"><div><h1>Análise</h1><span className="muted">Filtra e compara o teu histórico por vários critérios.</span></div><button className="primary" onClick={run}>{loading?'A calcular…':'Atualizar'}</button></div><section><div className="analysis-filters"><label>Casa<select value={f.house_id||''} onChange={e=>setF({...f,house_id:e.target.value})}><option value="">Todas</option>{houses.map(h=><option key={h.id} value={h.id}>{h.name}</option>)}</select></label><label>Mercado<select value={f.market||''} onChange={e=>setF({...f,market:e.target.value})}><option value="">Todos</option>{markets.map(m=><option key={m.id}>{m.name}</option>)}</select></label><label>País<select value={f.country||''} onChange={e=>setF({...f,country:e.target.value})}><option value="">Todos</option>{countries.map(c=><option key={c.id}>{c.name}</option>)}</select></label><label>Competição<select value={f.competition||''} onChange={e=>setF({...f,competition:e.target.value})}><option value="">Todas</option>{competitions.map(c=><option key={c.id}>{c.name}</option>)}</select></label><label>Equipa<input list="analysis-teams" value={f.team||''} onChange={e=>setF({...f,team:e.target.value})}/><datalist id="analysis-teams">{[...new Set(bets.flatMap(b=>[b.home_team,b.away_team]).filter(Boolean))].sort().map(t=><option key={t}>{t}</option>)}</datalist></label><label>Odds mín.<input type="number" step="0.01" value={f.min_odds||''} onChange={e=>setF({...f,min_odds:e.target.value})}/></label><label>Odds máx.<input type="number" step="0.01" value={f.max_odds||''} onChange={e=>setF({...f,max_odds:e.target.value})}/></label><label>Últimos jogos<select value={f.limit} onChange={e=>setF({...f,limit:e.target.value})}><option value="5">5</option><option value="10">10</option><option value="20">20</option></select></label></div></section>{data&&<><section><div className="cards"><div className="card"><small>Apostas</small><strong>{data.summary.bets}</strong></div><div className="card"><small>Stake</small><strong>{fmt(data.summary.stake)} €</strong></div><div className="card"><small>P/L</small><strong>{fmt(data.summary.pnl)} €</strong></div><div className="card"><small>ROI</small><strong>{fmt(data.summary.roi)}%</strong></div><div className="card"><small>Win rate</small><strong>{fmt(data.summary.winRate)}%</strong></div></div></section><Table title="Por mercado" rows={data.byMarket}/><Table title="Por país" rows={data.byCountry}/><Table title="Por competição" rows={data.byCompetition}/><Table title="Por casa" rows={data.byHouse}/><Table title="Por equipa" rows={data.teamStats}/>{f.team&&<section><h2>Últimos {f.limit} jogos de {f.team}</h2>{data.recent.map((r,i)=><div className="row" key={i}>{r.datetime?.replace('T',' ')} · {r.home_team} {r.away_team} · {r.market} · <b>{r.result==='win'?'GREEN':r.result==='loss'?'RED':r.result}</b> · {fmt(r.profit)} €</div>)}</section>}</>}</>}
function Admin(){const[users,setUsers]=useState([]),[error,setError]=useState('');const load=()=>api('/admin/users').then(setUsers).catch(e=>setError(e.message));useEffect(load,[]);const update=(id,body)=>api('/admin/users/'+id,{method:'PATCH',body:JSON.stringify(body)}).then(load).catch(e=>setError(e.message));return <section><h1>Gestão de utilizadores</h1>{error&&<div className="error">{error}</div>}<div className="tablewrap"><table><thead><tr><th>Nome</th><th>Email</th><th>Perfil</th><th>Estado</th><th>Ações</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td><select value={u.role} onChange={e=>update(u.id,{role:e.target.value})}><option value="user">Utilizador</option><option value="admin">Admin</option></select></td><td>{u.active?'Ativo':'Bloqueado'}</td><td><button onClick={()=>update(u.id,{active:!u.active})}>{u.active?'Bloquear':'Ativar'}</button> <button onClick={()=>update(u.id,{resetSessions:true})}>Terminar sessões</button></td></tr>)}</tbody></table></div></section>}
function Config(){const[countries,setCountries]=useState([]),[comps,setComps]=useState([]),[markets,setMarkets]=useState([]),[c,setC]=useState({}),[co,setCo]=useState({}),[m,setM]=useState({}),[error,setError]=useState('');const load=()=>Promise.all([api('/admin/countries'),api('/admin/competitions'),api('/admin/markets')]).then(([a,b,d])=>{setCountries(a);setComps(b);setMarkets(d)}).catch(e=>setError(e.message));useEffect(load,[]);const act=(url,method,body)=>api(url,{method,body:JSON.stringify(body)}).then(load).catch(e=>setError(e.message));return <><PasswordPanel/><section><h1>Configuração de futebol</h1><p className="muted">A gestão destas listas é exclusiva do administrador.</p>{error&&<div className="error">{error}</div>}<h2>Países</h2><div className="inline"><input placeholder="Nome" value={c.name||''} onChange={e=>setC({...c,name:e.target.value})}/><input placeholder="URL do logotipo/bandeira" value={c.logo||''} onChange={e=>setC({...c,logo:e.target.value})}/><button className="primary" onClick={()=>act('/admin/countries','POST',c).then(()=>setC({}))}>Adicionar</button></div><div className="chips">{countries.map(x=><span className="chip" key={x.id}>{x.logo&&<img src={x.logo}/>} {x.name} <button onClick={()=>act('/admin/countries/'+x.id,'DELETE',{})}>×</button></span>)}</div><h2>Competições</h2><div className="inline"><select value={co.country_id||''} onChange={e=>setCo({...co,country_id:e.target.value})}><option value="">País</option>{countries.map(x=><option value={x.id}>{x.name}</option>)}</select><input placeholder="Nome da competição" value={co.name||''} onChange={e=>setCo({...co,name:e.target.value})}/><input placeholder="URL do logotipo (opcional)" value={co.logo||''} onChange={e=>setCo({...co,logo:e.target.value})}/><button className="primary" onClick={()=>act('/admin/competitions','POST',co).then(()=>setCo({}))}>Adicionar</button></div><div className="chips">{comps.map(x=><span className="chip" key={x.id}>{x.logo&&<img src={x.logo}/>} {x.country_name} · {x.name} <button onClick={()=>act('/admin/competitions/'+x.id,'DELETE',{})}>×</button></span>)}</div><h2>Mercados</h2><div className="inline"><input placeholder="Ex.: Mais de 2.5 golos" value={m.name||''} onChange={e=>setM({...m,name:e.target.value})}/><button className="primary" onClick={()=>act('/admin/markets','POST',m).then(()=>setM({}))}>Adicionar</button></div><div className="chips">{markets.map(x=><span className="chip" key={x.id}>{x.name} <button onClick={()=>act('/admin/markets/'+x.id,'DELETE',{})}>×</button></span>)}</div></section></>}
function PasswordPanel(){const[x,setX]=useState({}),[msg,setMsg]=useState('');const f=(k,v)=>setX({...x,[k]:v});return <section><h2>Segurança da conta</h2><div className="grid"><Field label="Password atual" k="currentPassword" type="password" x={x} f={f}/><Field label="Nova password" k="newPassword" type="password" x={x} f={f}/></div><button className="primary" onClick={()=>api('/auth/change-password',{method:'POST',body:JSON.stringify(x)}).then(()=>{setMsg('Password alterada com sucesso.');setX({})}).catch(e=>setMsg(e.message))}>Alterar password</button>{msg&&<p className="muted">{msg}</p>}</section>}
function Dashboard({stats,bets,houses}){const[house,setHouse]=useState(''),[period,setPeriod]=useState('month');const d=new Date(),from=period==='week'?new Date(d.getTime()-6*86400000):period==='month'?new Date(d.getTime()-29*86400000):null;const filtered=bets.filter(x=>(!house||String(x.house_id)===house)&&(!from||String(x.datetime||'').slice(0,10)>=from.toISOString().slice(0,10))&&x.result!=='pending');const stake=filtered.reduce((a,x)=>a+Number(x.stake||0),0),pnl=filtered.reduce((a,x)=>a+Number(x.profit||0),0),wins=filtered.filter(x=>x.result==='win').length;return <><div className="dashboard-head"><div><h1>Dashboard</h1><span className="muted">Visão geral da tua banca e desempenho</span></div><div className="filters"><select value={period} onChange={e=>setPeriod(e.target.value)}><option value="week">Últimos 7 dias</option><option value="month">Últimos 30 dias</option><option value="all">Tudo</option></select><select value={house} onChange={e=>setHouse(e.target.value)}><option value="">Todas as casas</option>{houses.map(h=><option key={h.id} value={h.id}>{h.name}</option>)}</select></div></div><div className="cards"><div className="card accent"><small>Saldo total</small><strong>{Number(stats.balance||0).toFixed(2)} €</strong></div><div className="card"><small>P&amp;L</small><strong>{pnl.toFixed(2)} €</strong></div><div className="card"><small>ROI</small><strong>{(stake?pnl/stake*100:0).toFixed(2)}%</strong></div><div className="card"><small>Win rate</small><strong>{(filtered.length?wins/filtered.length*100:0).toFixed(1)}%</strong></div><div className="card"><small>Apostas</small><strong>{filtered.length}</strong></div><div className="card"><small>Stake</small><strong>{stake.toFixed(2)} €</strong></div></div><section><h2>Evolução do P&amp;L</h2><div className="chart"><ResponsiveContainer><LineChart data={filtered.slice().reverse().map((x,i)=>({i:i+1,p:Number(x.profit||0)}))}><XAxis dataKey="i"/><YAxis/><Tooltip/><Line type="monotone" dataKey="p"/></LineChart></ResponsiveContainer></div></section></>}function CountryField({countries,x,f}){const selected=countries.find(c=>c.name===x.country);return <label className="country-field">País<div className="country-select-wrap"><select value={x.country??''} onChange={e=>f('country',e.target.value)}><option value="">—</option>{countries.map(c=><option key={c.id} value={c.name}>{c.logo?c.logo+' ':''}{c.name}</option>)}</select>{selected?.logo&&<span className="country-flag" aria-label={selected.name}>{selected.logo}</span>}</div></label>}
function BetForm({bets=[],houses,countries,competitions,markets,done,edit}){const[x,setX]=useState(edit||{datetime:now(),odds:1.4,stake:10,units:1,result:'pending',bonus:false,bet_type:'single',selections:[{event:'',market:'',selection:'',odds:1.4}],combined_odds:1.4});const[f,setF]=useState(k=>v=>setX(a=>({...a,[k]:v})));useEffect(()=>{if(edit)setX({...edit,selections:edit.selections||[]})},[edit]);const save=()=>api(edit?'/bets/'+edit.id:'/bets',{method:edit?'PUT':'POST',body:JSON.stringify(x)}).then(done);const changeSel=(i,k,v)=>setX(a=>({...a,selections:a.selections.map((s,j)=>j===i?{...s,[k]:v}:s)}));const addSel=()=>setX(a=>({...a,selections:[...(a.selections||[]),{event:'',market:'',selection:'',odds:1.4}]}));return <section><h1>{edit?'Editar aposta':'Nova aposta'}</h1><div className="grid"><Field label="Tipo" k="bet_type" x={x} f={f} type="select" opts={[['single','Simples'],['multiple','Múltipla']]}/><Field label="Data/hora" k="datetime" x={x} f={f} type="datetime-local"/><Field label="Casa" k="house_id" x={x} f={f} type="select" opts={houses.map(h=>[h.id,h.name])}/><CountryField countries={countries} x={x} f={f}/><Field label="Competição" k="competition" x={x} f={f} type="select" opts={competitions.filter(c=>!x.country||c.country_name===x.country).map(c=>[c.name,c.name])}/><label>Equipa casa<input list="teams-list" value={x.home_team||""} onChange={e=>f("home_team",e.target.value)}/></label><label>Equipa fora<input list="teams-list" value={x.away_team||""} onChange={e=>f("away_team",e.target.value)}/></label><datalist id="teams-list">{[...new Set(bets.flatMap(b=>[b.home_team,b.away_team]).filter(Boolean))].sort().map(t=><option key={t} value={t}/>)}</datalist><Field label="Mercado" k="market" x={x} f={f} type="select" opts={markets.map(m=>[m.name,m.name])}/><Field label="Seleção" k="selection" x={x} f={f}/><Field label="Odds" k="odds" x={x} f={f} type="number"/><Field label="Stake €" k="stake" x={x} f={f} type="number"/><Field label="Unidades" k="units" x={x} f={f} type="number"/><Field label="Resultado" k="result" x={x} f={f} type="select" opts={[['pending','Pendente'],['win','Green'],['loss','Red'],['push','Push'],['void','Void']]}/><Field label="P/L €" k="profit" x={x} f={f} type="number"/><Field label="Notas" k="notes" x={x} f={f} type="textarea"/></div>{x.bet_type==='multiple'&&<div className="multi"><h3>Seleções da múltipla</h3>{(x.selections||[]).map((s,i)=><div className="selection-row" key={i}><input placeholder="Jogo/evento" value={s.event||''} onChange={e=>changeSel(i,'event',e.target.value)}/><input placeholder="Mercado" value={s.market||''} onChange={e=>changeSel(i,'market',e.target.value)}/><input placeholder="Seleção" value={s.selection||''} onChange={e=>changeSel(i,'selection',e.target.value)}/><input type="number" placeholder="Odds" value={s.odds||''} onChange={e=>changeSel(i,'odds',Number(e.target.value))}/></div>)}<button onClick={addSel}>+ Adicionar seleção</button><Field label="Odds combinadas" k="combined_odds" x={x} f={f} type="number"/></div>}<label className="check"><input type="checkbox" checked={!!x.bonus} onChange={e=>f('bonus',e.target.checked)}/> Conta para bónus</label><button className="primary" onClick={save}>Guardar</button></section>}
function BetList({bets,houses,countries,competitions,markets,done}){const[edit,setEdit]=useState(null);return <>{edit&&<BetForm houses={houses} countries={countries} competitions={competitions} markets={markets} edit={edit} done={()=>{setEdit(null);done()}}/>}<section><h1>Apostas</h1><div className="tablewrap"><table><thead><tr>{['Data','Casa','Jogo','Mercado','Seleção','Odds','Stake','Resultado','P/L','Ações'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{bets.map(b=><tr key={b.id}><td>{b.datetime?.replace('T',' ')}</td><td>{b.house_name||'—'}</td><td>{b.home_team} — {b.away_team}</td><td>{b.market}</td><td>{b.selection}{b.bet_type==='multiple'?' · Múltipla':''}</td><td>{b.combined_odds||b.odds}</td><td>{b.stake} €</td><td><b className={'result '+b.result}>{b.result==='win'?'GREEN':b.result==='loss'?'RED':b.result}</b></td><td>{Number(b.profit||0).toFixed(2)} €</td><td><button onClick={()=>setEdit({...b,selections:JSON.parse(b.selections||'[]')})}>Editar</button></td></tr>)}</tbody></table></div></section></>}
function HouseForm({houses,done}){const[x,setX]=useState({});const f=(k,v)=>setX({...x,[k]:v});return <><section><h1>Casas de apostas</h1><div className="grid">{[['name','Nome'],['username','Utilizador'],['logo','URL do logotipo'],['bonus_day','Dia do bónus'],['bonus_conditions','Condições','textarea']].map(z=><Field key={z[0]} label={z[1]} k={z[0]} type={z[2]} x={x} f={f}/>)}</div><button className="primary" onClick={()=>api('/houses',{method:'POST',body:JSON.stringify(x)}).then(()=>{setX({});done()})}>Adicionar casa</button></section>{houses.map(h=><section key={h.id} className="house-card">{h.logo&&<img className="logo" src={h.logo}/>}<div><b>{h.name}</b> · {h.username}<p>{h.bonus_day} · {h.bonus_conditions}</p></div></section>)}</>}
function Money({houses,tx,done}){const[x,setX]=useState({datetime:now(),status:'completed',type:'deposit'}),f=(k,v)=>setX({...x,[k]:v});const fs=[['datetime','Data/hora','datetime-local'],['house_id','Casa','select',houses.map(h=>[h.id,h.name])],['type','Tipo','select',[['deposit','Depósito'],['withdrawal','Levantamento'],['bonus','Bónus'],['adjustment','Ajuste'],['other','Outro']]],['amount','Valor €','number'],['method','Método'],['status','Estado'],['notes','Notas','textarea']];return <><section><h1>Movimentos</h1><div className="grid">{fs.map(z=><Field key={z[0]} label={z[1]} k={z[0]} type={z[2]} opts={z[3]} x={x} f={f}/>)}</div><button className="primary" onClick={()=>api('/transactions',{method:'POST',body:JSON.stringify(x)}).then(()=>{setX({datetime:now(),status:'completed',type:'deposit'});done()})}>Registar</button></section><section>{tx.map(x=><div className="row" key={x.id}>{x.datetime} · {x.house_name||'—'} · {x.type} · {x.amount} € · {x.method}</div>)}</section></>}
function Bonus({houses,bonuses,done}){const[x,setX]=useState({required_events:3,min_odds:1.4,min_combined_odds:3,progress:0,status:'open'}),f=(k,v)=>setX({...x,[k]:v});const fs=[['house_id','Casa','select',houses.map(h=>[h.id,h.name])],['week_start','Semana'],['deadline','Prazo','datetime-local'],['required_events','Eventos necessários','number'],['min_odds','Odds mínimas','number'],['min_combined_odds','Odds combinadas','number'],['progress','Progresso','number'],['status','Estado'],['notes','Notas','textarea']];return <><section><h1>Bónus semanal</h1><div className="grid">{fs.map(z=><Field key={z[0]} label={z[1]} k={z[0]} type={z[2]} opts={z[3]} x={x} f={f}/>)}</div><button className="primary" onClick={()=>api('/bonuses',{method:'POST',body:JSON.stringify(x)}).then(()=>{setX({required_events:3,min_odds:1.4,min_combined_odds:3,progress:0,status:'open'});done()})}>Adicionar bónus</button></section>{bonuses.map(b=><section key={b.id}><b>{b.house_name||'Casa'} · {b.progress}/{b.required_events}</b><p>mín. odds {b.min_odds} · combinada {b.min_combined_odds} · prazo {b.deadline}</p></section>)}</>}
createRoot(document.getElementById('root')).render(<App/>);