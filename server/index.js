const express=require('express');const Database=require('better-sqlite3');const path=require('path');const fs=require('fs');
const app=express();app.use(express.json());const dataDir=process.env.DATA_DIR||'/app/data';fs.mkdirSync(dataDir,{recursive:true});const db=new Database(path.join(dataDir,'betting.db'));db.pragma('journal_mode = WAL');
db.exec(`CREATE TABLE IF NOT EXISTS houses(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,username TEXT,password TEXT,bonus_day TEXT,bonus_conditions TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE IF NOT EXISTS bets(id INTEGER PRIMARY KEY AUTOINCREMENT,datetime TEXT,house_id INTEGER,country TEXT,competition TEXT,home_team TEXT,away_team TEXT,market TEXT,selection TEXT,odds REAL,stake REAL,units REAL,bonus INTEGER DEFAULT 0,result TEXT DEFAULT 'pending',profit REAL DEFAULT 0,notes TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(house_id) REFERENCES houses(id));
CREATE TABLE IF NOT EXISTS transactions(id INTEGER PRIMARY KEY AUTOINCREMENT,house_id INTEGER,type TEXT,amount REAL,method TEXT,status TEXT,datetime TEXT,notes TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(house_id) REFERENCES houses(id));
CREATE TABLE IF NOT EXISTS bonuses(id INTEGER PRIMARY KEY AUTOINCREMENT,house_id INTEGER,week_start TEXT,deadline TEXT,required_events INTEGER, min_odds REAL,min_combined_odds REAL,progress INTEGER DEFAULT 0,status TEXT DEFAULT 'open',notes TEXT,FOREIGN KEY(house_id) REFERENCES houses(id));`);
const all=(s,...a)=>db.prepare(s).all(...a),run=(s,...a)=>db.prepare(s).run(...a);
app.get('/api/houses',(_,res)=>res.json(all('SELECT id,name,username,bonus_day,bonus_conditions,created_at FROM houses ORDER BY name')));
app.post('/api/houses',(req,res)=>{const x=req.body;run('INSERT INTO houses(name,username,password,bonus_day,bonus_conditions) VALUES(?,?,?,?,?)',x.name,x.username||'',x.password||'',x.bonus_day||'',x.bonus_conditions||'');res.json({ok:true})});
app.get('/api/bets',(_,res)=>res.json(all('SELECT b.*,h.name house_name FROM bets b LEFT JOIN houses h ON h.id=b.house_id ORDER BY datetime(b.datetime) DESC,b.id DESC')));
app.post('/api/bets',(req,res)=>{const x=req.body;run('INSERT INTO bets(datetime,house_id,country,competition,home_team,away_team,market,selection,odds,stake,units,bonus,result,profit,notes) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)',x.datetime,x.house_id||null,x.country||'',x.competition||'',x.home_team||'',x.away_team||'',x.market||'',x.selection||'',Number(x.odds)||0,Number(x.stake)||0,Number(x.units)||0,x.bonus?1:0,x.result||'pending',Number(x.profit)||0,x.notes||'');res.json({ok:true})});
app.put('/api/bets/:id',(req,res)=>{const x=req.body;run('UPDATE bets SET result=?,profit=?,notes=? WHERE id=?',x.result,x.profit,x.notes||'',req.params.id);res.json({ok:true})});
app.delete('/api/bets/:id',(req,res)=>{run('DELETE FROM bets WHERE id=?',req.params.id);res.json({ok:true})});
app.get('/api/transactions',(_,res)=>res.json(all('SELECT t.*,h.name house_name FROM transactions t LEFT JOIN houses h ON h.id=t.house_id ORDER BY datetime(t.datetime) DESC,t.id DESC')));
app.post('/api/transactions',(req,res)=>{const x=req.body;run('INSERT INTO transactions(house_id,type,amount,method,status,datetime,notes) VALUES(?,?,?,?,?,?,?)',x.house_id||null,x.type,x.amount,x.method||'',x.status||'completed',x.datetime,x.notes||'');res.json({ok:true})});
app.get('/api/bonuses',(_,res)=>res.json(all('SELECT b.*,h.name house_name FROM bonuses b LEFT JOIN houses h ON h.id=b.house_id ORDER BY deadline')));
app.post('/api/bonuses',(req,res)=>{const x=req.body;run('INSERT INTO bonuses(house_id,week_start,deadline,required_events,min_odds,min_combined_odds,progress,status,notes) VALUES(?,?,?,?,?,?,?,?,?)',x.house_id,x.week_start,x.deadline,x.required_events,x.min_odds,x.min_combined_odds,x.progress||0,x.status||'open',x.notes||'');res.json({ok:true})});
app.put('/api/bonuses/:id',(req,res)=>{const x=req.body;run('UPDATE bonuses SET progress=?,status=?,notes=? WHERE id=?',x.progress,x.status,x.notes||'',req.params.id);res.json({ok:true})});
app.get('/api/stats',(_,res)=>{const bets=all('SELECT * FROM bets'),resolved=bets.filter(x=>x.result!=='pending'),stake=resolved.reduce((s,x)=>s+Number(x.stake||0),0),pnl=resolved.reduce((s,x)=>s+Number(x.profit||0),0),wins=resolved.filter(x=>x.result==='win').length,tx=all(`SELECT COALESCE(SUM(CASE WHEN type IN ('deposit','bonus','adjustment') THEN amount WHEN type='withdrawal' THEN -amount ELSE 0 END),0) balance FROM transactions`)[0].balance;res.json({count:bets.length,resolved:resolved.length,pnl,stake,roi:stake?pnl/stake*100:0,winRate:resolved.length?wins/resolved.length*100:0,balance:Number(tx)+pnl})});
app.get('/api/export',(req,res)=>{const rows=all('SELECT b.datetime,h.name house,b.country,b.competition,b.home_team,b.away_team,b.market,b.selection,b.odds,b.stake,b.units,b.bonus,b.result,b.profit,b.notes FROM bets b LEFT JOIN houses h ON h.id=b.house_id ORDER BY b.datetime');const cols=['datetime','house','country','competition','home_team','away_team','market','selection','odds','stake','units','bonus','result','profit','notes'];const esc=v=>'"'+String(v??'').replaceAll('"','""')+'"';res.type('text/csv').send([cols.join(','),...rows.map(x=>cols.map(c=>esc(x[c])).join(','))].join('\n'))});
app.use(express.static(path.join(__dirname,'..','dist')));app.use((req,res)=>res.sendFile(path.join(__dirname,'..','dist','index.html')));app.listen(process.env.PORT||3000,'0.0.0.0',()=>console.log('BetTracker running'));